from setuptools import setup

setup(name='chess',
        version='0.1',
        description='does cool stuff with chess',
        author='Diarmuid Morgan',
        packages=['chess', 'chess.pieces'],
        include_package_data=True,
        )
